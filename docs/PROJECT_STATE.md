# Project State

Canonical project tracker. This file is the authoritative source for project state visible to agents and humans. The orchestrator branch never merges; this file does.

## 1. Owner Vision & Scope Boundaries

- **Product Vision:** Fake Chan Factory is a zero-backend static GitHub Pages reader for Classical Chinese Chan (Zen) literature (Tang/Song yulu, denglu, gongan). It presents English-first walnut-hall interface (gate: lintel + directory + beam hook "The old texts are real. The translators are not."), 5 public rooms only (Reader, Comparative Matrix, Lineage, Gong'an Index, Chan Lexicon), bilingual reading with source Chinese as largest text in Reader, multi-translator comparative alignment with explicit Robo/AI disclosure, lineage graph, gongan catalogue, lexicon. The corpus references CBETA/Taishō witnesses (T47, T48, T51, X-series) with honest source-review states (collated_to_claimed_witness, partial_or_failed_w1_collation, witness_unavailable). The project is playfully honest: most English renderings are 🤖 Robo — AI text in a famous translator's register, not citable as their work. A ✅ Edition-verified quotation badge means wording checked against recorded edition; rights review is separate.

- **Scope Boundaries (Non-Goals):**
  - Will NOT build Translation Studio, Arena AI Agents, header GitHub link — public scope stays exactly 5 rooms, smoke-guarded.
  - Will NOT generate source-looking Classical Chinese — corpus text comes only from pinned, digest-verified CBETA witnesses.
  - Will NOT approve rights — edition verification ≠ rights approval; rights_manifest.json sources await human/jurisdiction review.
  - Will NOT edit `.github/workflows/*` without explicit owner approval; OPERATIONS.md is sole register of owner-controlled CI changes.
  - Will NOT fetch/transcribe/evaluate OUT-OF-CBETA witnesses via agents — 31-document human-sourcing queue is human work only (Ruling 4, 2026-09-12).
  - Will NOT treat representation count (N/N cases) as completion — only explicit `completion_status: complete_selected_witness` + `collated_to_claimed_witness` + unit_targets met counts, per validator.
  - Will NOT edit `docs/` by hand — `docs/` is generated by `scripts/build_data_bundle.py` (mirror list); hand-editing is editing build artifact at wrong end.
  - Will NOT edit checker to make gate pass — when gate and prose disagree, prose is wrong; fix document, not validator/preservation/rules.
  - Will NOT re-point withdrawn false claims at unverified candidates — Ruling 3: false T1987 Zhaozhou claim withdrawn, not re-pointed.

## 2. Architectural Invariants

- **Pipeline order fixed:** `data/` → `scripts/validate_data.py` → `data/project_metrics.json` → `scripts/build_data_bundle.py` → root assets + byte-identical `/docs` mirror → GitHub Pages publishes `main/docs`.
- **Five quality gates pass before every push:** `python3 -m py_compile scripts/*.py` · `python3 scripts/validate_data.py` · `python3 scripts/build_data_bundle.py` · `python3 scripts/test_source_preservation.py` (allowlist, no unauthorized Chinese edits) · `python3 scripts/test_source_review_rules.py` (138+ checks, census pins, W1 evidence) · `node scripts/smoke_test.mjs` (dependency-free reader) · `git diff --exit-code -- app_data.js docs/app_data.js docs/data data/project_metrics.json`.
- **Gate execution:** unprivileged disposable clone, stdlib-only, read-only, exits 0 only when all checks pass. Minimum Python 3.11, tested 3.11/3.12/3.13, CI pins 3.12 in `.github/workflows/quality.yml`. Manifest checksums: every file in `data/corpus_manifest.json` must be verified by SHA-256; manifest nobody verifies proves nothing. Atomic ready marker checked last. See `GATE.md`.
- **Determinism:** `build_data_bundle.py` run twice must produce byte-identical `app_data.js` and `docs/app_data.js` (current hash `937e5467fefe661773f4c0ad5397399963612794d5becf341865d0bee242071b`, 8,143,477 bytes, 17 docs).
- **Source preservation:** `test_source_preservation.py` byte-compares `data/corpus/` against pinned base `3cc7a8e9681e` permitting only allowlisted exact JSON pointers (currently 218 permitted changes, 0 unauthorized).
- **Internal identifiers stay:** `translatechan_*` localStorage keys, `window.TranslateChan`, `TRANSLATECHAN_DATA`; public brand "Fake Chan Factory" and humor-forward tone stays.
- **Sessions append-only:** `sessions/` dated evidence never edited/deleted; new dated record supersedes old. `sessions/COLLATION_REGISTER_2026-09-09.json` historical, `sessions/COLLATION_REGISTER_2026-09-10_CORRECTION.json` correction, current authoritative `sessions/COLLATION_REGISTER_2026-09-22_P1_BIYANLU.json` (17 docs, 127 flagged, collated 13 partial 2 unavailable 2).
- **Provenance labels:** three passage-level keys precedence `recension_note` → `editorial_note` → `cbeta_note`, one line each, rendered at 17 content sites; `coverage_note` is document-scale dossier ledger, single rendering as "Reading" row, sole explained exemption in `NOTE_RENDER_EXEMPTIONS`. Census current: 47 total note strings (cbeta 16, editorial 16, coverage 15, recension 0), 32 rendered beside passage, 16 documents carry label.
- **Completion rule:** `complete` ⇔ `complete_selected_witness` + `collated_to_claimed_witness` + unit_targets met (validator, `complete_document_keys()`, `per_text_metrics()`, Reader, shelf, smoke + runtime checks).
- **Witness rule:** Cite no work as text's witness unless ≥1 evaluated content field matches it — reference merely listed in `WITNESS` map is bibliographic pairing, not collation evidence. Current exception: none (previous dahui_hongzhi exception resolved).
- **R-A/R-B/R-C policy:** R-A = re-key verbatim from pinned witness where carrier exists; R-B = label as project retelling with `editorial_note`/`coverage_note` where no carrier; R-C = human work for OUT-OF-CBETA.

## 3. Settled Decisions & Rationale

- **Four 2026-09-12 owner rulings (PR #43, still binding):**
  - Ruling 1: six CITATION rows ship in ONE PR (done PR #43). Forbids batching other remediation classes into that PR.
  - Ruling 2: ledger stays as is — 630 remains authoritative register figure and 2026-09-12 post-remediation record stays dated measurement beside it. Forbids re-designation, no `w1_evidence.py` or `FIXED_METADATA` change.
  - Ruling 3: fabricated/unsupported text replaced with real source where pinned witness carries it, labelled where not. Forbids re-pointing withdrawn claim at unverified candidate (Zhaozhou T1987 withdrawn, not re-pointed to X68n1315).
  - Ruling 4: OUT-OF-CBETA sourcing is human work — forbids agent fetch/transcription/evaluation of 31-document human-sourcing queue.
- **W1 evidence contract hardened (PRs #25–#28):** `w1_evidence.py` independently recomputes every derived figure vs stored ones, per-doc field arithmetic, reference provenance against both digest manifests, aggregate block, historical vs authoritative comparison; `--write-metrics` with blocking error exits nonzero and leaves metrics byte-identical.
- **Pages revamp Phases 1-3 complete (main 3a6ae32):** tokens 63→43, serif Source Serif 4, shell/hero/mobile bar, OG redraw, Reader minimal sheet with ledger drawer and thin register, secondary rooms re-composed as collation table / transmission register / case catalogue / dictionary, 0 style= attributes, CSP without unsafe-inline, bundle 1.9MB raw <2MB, render-lazy boot. No real-browser evidence yet — owner review pending.
- **Purge retellings (12 docs only) — Q5 consideration:** owner ruled 2026-09-21 to purge 12 retellings that were project compositions with no witness, leaving 12 docs only, then re-key Tier1 missing. Implemented: main 5d44864 has 16 docs (after enthusiast fulls), current PR adds Biyanlu as 17th.
- **Amendment 2026-09-21 second round:** git clone at manifest commit is official delivery; system `python3` for gate, no container. Requirements (a) sha256 verification mandatory, (b) gate unprivileged disposable stdlib-only read-only, (c) GATE.md states minimum version and tested set.
- **Biyanlu re-key (task 057, PR #113):** 100 cases from pinned T48n2003 verbatim tiling, 400/400 content fields EXACT per register, 86 metadata flagged, zh_chars 103020 = content_cjk_count, cbeta_note T2003, coverage_note honest, deterministic bundle hash preserved.
- **Q7 W3 wiki only 100% collated:** owner consideration that wiki/margin feature should only show for 100% collated documents — lane split: work=Book fascicle=Chapter passage=Page.

## 4. Active Milestone & Current State

- **Active Milestone:** P1 — Restore Tier1 missing (Biyanlu) + make 10 partial docs complete_selected_witness. W1 COMPLETE (2026-09-09), CORRECTED (2026-09-10), now in remediation: Wave 1 (wumenguan, biyanlu_cases, linji_yulu, xinxin_ming, platform_sutra) done, enthusiast fulls (caoshan_benji, chuandenglu_full, congronglu, huangbo_fayao_full, mazu_guanglu_full, yunmen_guanglu_full, dongshan_yulu_full, zhaozhou_yulu_full, dahui_yulu_full) done, P2 Tier2 batch1 pending, lineage expansion pending.

- **Current State (as of 2026-09-22, PR #113, main 5d44864 + biyanlu):**
  - **Corpus:** 17 documents, 12 complete_selected_witness (biyanlu_cases, wumenguan, linji_yulu, congronglu, chuandenglu_full, caoshan_benji, huangbo_fayao_full, mazu_guanglu_full, yunmen_guanglu_full, dongshan_yulu_full, zhaozhou_yulu_full, dahui_yulu_full), 5 excerpt_seed (zhengdao_ge, hanshan_poems, niutou_juezhu, guiyang_yulu, fayan_yulu). Content CJK **878,582** / all-string CJK **937,884**.
  - **Translations:** 209 corpus slots, 2 verified_quotation slots across 2 corpus texts + 2 verified comparative-matrix entries, 21 matrix registers. Verified-reference coverage **3 / 4** (3 recorded / 1 pending).
  - **Locators:** 4192/4192 case-level, 6 document-level seeds.
  - **Source review:** collated_to_claimed_witness 13, partial_or_failed 2, witness_unavailable 2, flagged 127, evidence 2026-09-22 → `sessions/COLLATION_REGISTER_2026-09-22_P1_BIYANLU.json`.
  - **Provenance:** 47 total note strings (cbeta 16, editorial 16, coverage 15, recension 0), 32 rendered, 16 docs carry label.
  - **Bundle:** `app_data.js` 8,143,477 bytes, deterministic hash `937e5467fefe661773f4c0ad5397399963612794d5becf341865d0bee242071b` twice, root/docs byte-identical.
  - **Gates:** py_compile PASS, validate_data PASS, build_data_bundle deterministic, preservation 0 unauthorized, rules PASS, smoke PASS 17 texts, mirror clean.
  - **PRs merged since W1:** #29 wumenguan re-key, #30 biyanlu (first), #32 linji, #34 xinxin_ming, #35 platform_sutra label-only, #43 citation fixes, #46-#50 Pages revamp, #108 wumenguan+linji combined overlay (14 docs), #112 tier2 batch1 (16 docs), #113 biyanlu re-key (17 docs, this PR).
  - **Open PR:** #113 P1 Biyanlu re-key — ready to merge, gates green.

- **Immediate Next Task:** `058-p1-complete-marking` — Make 10 partial docs complete_selected_witness with honest unit_targets. Base main after PR #113 (17 docs), target branch `fix/p1-complete-marking`. Deliverables: `data/corpus_manifest.json` updated with unit_targets and completion_status complete_selected_witness for 10 docs where W1 status collated_to_claimed_witness and unit counts meet targets, `data/project_metrics.json` regenerated, `app_data.js` deterministic, report `sessions/P1_COMPLETE_MARKING_2026-09-22.md`. Quality checks: py_compile PASS, validate PASS corpus 17, build PASS deterministic, preservation allowlist extended only with exact pointers, rules PASS, smoke PASS, mirror clean. See `.orchestrator/prompts/058-p1-complete-marking.md`.

- **Following queue:**
  - [ ] 059-p2-tier2-yulu-batch1 — Guiyang + Fayan authenticity labels or re-key (R-A if ≥80% carrier in X-series else R-B per 047 pattern)
  - [ ] 060-p3-lineage-expansion-150 — 25 new masters from Guzunsu yulu to reach 60, update lineage_verification with exact locators for 10 edges
  - [ ] Later: 31 lineage edges exact-locator groundwork, W2 verified-quotation spot-check, real-browser verification pass (frozen), rights review (human)

## 5. Deferred / Technical Debt (carried from STATE.md)

- Congronglu front matter and interlinear 著語 apparatus, gongan indexing — still open
- Caoshan Benji sibling Taishō part T47n1987B cross-referenced as probe, not merged
- Translations for enthusiast fulls (caoshan, chuandenglu_full, etc.) — human editorial sign-off pending
- 31/31 lineage edges remain `traditional_link_pending_exact_locator`; corpus-key curation complete for 6-profile scope, later exact-locator work remains
- Real-browser screenshot/accessibility evidence unavailable (Chromium ECONNRESET 2026-08-11)
- Branch protection on main unconfirmed (403)
- JSON Schema declarative only; Python validator is enforced contract
- Rights review — all rights_manifest sources await human/jurisdiction review

## 6. Handoff Notes (for successor orchestrator)

- **Continuation branch:** empty = new engagement. Previous orchestrator branch was `arena/01a09829-translatechan` (coding agent branch, not orchestrator distribution branch). Its work (PR #113) is now merged into main as of this file's commit.
- **Orchestrator branch model:** distribution channel, never merges, only `.orchestrator/prompts/*` + `.orchestrator/local/ORCHESTRATOR_STATE.md`. Current orchestrator branch should be created fresh after this handoff, or reuse existing if operator provides one. See `ORCHESTRATOR CORE v4.9.0` Phase 3.
- **Refresh triggers:** before authoring any task prompt, at every PR hand-back, moment operator reports merge — run `git fetch --depth 1 origin +main:refs/remotes/origin/main` + `git show origin/main:docs/PROJECT_STATE.md`.
- **Environment hazard:** Arena sandbox can rewind worktree to old SHA and drop GitHub credentials mid-session — confirm with `gh api repos/56eli/translatechan/commits/<sha>` and `git ls-remote`, never `git pull`, never force-push, never commit on main.
- **Next orchestrator:** read this file + `GATE.md` + `.orchestrator/STATE.md` (legacy, contains detailed dated evidence entries) + `AUDIT.md` + `HANDOFF.md` + `ROADMAP.md` + `vision.md` + `sessions/` + `scripts/validate_data.py` to rebuild mental model. Then present Phase 2 findings and seek alignment per v4.9.0.

